name: instagram
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  intl: ^0.19.0
  cupertino_icons: ^1.0.0
  flutter:
    sdk: flutter
  dio: ^5.7.0
  provider: ^6.1.2
  geolocator: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
    - assets/weathers/
